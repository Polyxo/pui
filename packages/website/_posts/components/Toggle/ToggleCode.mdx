---
category: "Core"
title: "Toggle"
excerpt: "**Toggles** are controls that are used to quickly switch between two possible states."
coverImage: "../sample-image.jpg"
slug: "Components/Forms/Toggle/tab:Code"
figma: "https://www.figma.com/file/KWrOuAzzykyysIDFpLikx1/WFP---Bridge?type=design&node-id=130%3A2249&mode=design&t=eFfGsO7k7LGjWFwo-1"
mainComponent: "Toggle"
---

The Toogle component is wrapped inside the `Input` component.

### react-hook-form example

```jsx live reactHookForm
<>
  <Toggle
    labelText="The uncontrolled Input"
    helperText="Optional helperText"
    labelA="Off"
    labelB="On"
    {...register("inputname")}
  />

  <Controller
    name="inputnameB"
    control={control}
    render={({ field }) => {
      return (
        <Toggle
          labelText="The controlled Input"
          helperText="Optional helperText"
          labelA="Off"
          labelB="On"
          {...field}
          onChange={(e) => field.onChange(e.target.checked)}
          checked={field.value === true}
        />
      );
    }}
  />
</>
```

Without label or helperText

```jsx live
<Toggle labelA="Off" labelB="On" />
```

### Reference

- [Carbon Design System Usage for text inputs](https://next.carbondesignsystem.com/components/text-input)

```

```
