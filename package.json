{
  "name": "@progressive-ui/designsystem",
  "version": "1.18.2",
  "description": "Progressive UI Design System",
  "license": "Apache-2.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@1.22.22",
  "engines": {
    "node": ">=24 <25",
    "yarn": ">=1.22.22 <2"
  },
  "scripts": {
    "start:react": "yarn workspace @progressiveui/react storybook",
    "build": "yarn build:packages && yarn build:website",
    "build:packages": "yarn generate:tokens && yarn workspace @progressiveui/styles build && yarn workspace @progressiveui/icons-core build && yarn generate:icons && yarn workspace @progressiveui/react build",
    "build:website": "yarn generate:website-assets && yarn workspace @progressiveui/core-website build",
    "build:storybook": "yarn workspace @progressiveui/react build:storybook",
    "generate": "yarn generate:tokens && yarn generate:icons",
    "generate:icons": "yarn workspace @progressiveui/icons-react build",
    "generate:test-assets": "yarn workspace @progressiveui/icons-core build && yarn generate:icons",
    "generate:tokens": "yarn workspace @progressiveui/themes-core build",
    "generate:typecheck-assets": "yarn generate:tokens && yarn workspace @progressiveui/react build:demos",
    "generate:website-assets": "yarn workspace @progressiveui/react build:demos && yarn workspace @progressiveui/react generate:website-metadata",
    "lint": "yarn workspace @progressiveui/react lint && yarn workspace @progressiveui/core-website lint",
    "test": "yarn generate:test-assets && yarn workspace @progressiveui/react test --runInBand",
    "typecheck": "yarn generate:typecheck-assets && yarn workspace @progressiveui/react typecheck && yarn workspace @progressiveui/core-website ts",
    "verify:packages": "node scripts/verify-packages.mjs",
    "verify:packed-packages": "node scripts/verify-packed-packages.mjs",
    "verify:public-exports": "node scripts/compare-public-exports.mjs",
    "verify:bundle-size": "node scripts/check-bundle-size.mjs",
    "verify:clean": "node scripts/verify-clean.mjs",
    "validate:quick": "yarn lint && yarn typecheck && yarn test",
    "validate:release": "yarn lint && yarn typecheck && yarn test && yarn build:packages && yarn build:storybook && yarn build:website && yarn verify:packages && yarn verify:packed-packages && yarn verify:public-exports && yarn verify:bundle-size && yarn verify:clean",
    "validate": "yarn validate:release",
    "publish:alpha": "yarn validate && lerna publish --dist-tag alpha --temp-tag",
    "publish:alpha-cli": "yarn validate && lerna publish --dist-tag alpha --temp-tag --no-verify-access --conventional-commits --yes",
    "lerna:publish": "yarn validate && lerna publish --conventional-commits --temp-tag",
    "lerna:publishFromPackage": "yarn validate && lerna publish from-package --temp-tag --yes",
    "lerna:publishFromPackageAlpha": "yarn validate && lerna publish from-package --dist-tag alpha --temp-tag --yes",
    "lerna:version": "yarn validate && lerna version --conventional-commits",
    "lerna:versionNoPush": "yarn validate && lerna version --no-push --conventional-commits --yes",
    "publish:main": "yarn validate && lerna publish --temp-tag"
  },
  "keywords": [
    "react",
    "ui-kit",
    "components"
  ],
  "devDependencies": {
    "lerna": "^8.2.4"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Polyxo/pui.git"
  },
  "bundleSizeThreshold": 80000
}
